apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: no-access-frontend-database
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: database
  policyTypes:
    - Ingress
  ingress:
    - from:
      - podSelector:
          matchLabels:
            app: backend
